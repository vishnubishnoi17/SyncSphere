version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: syncsphere
      POSTGRES_PASSWORD: syncsphere
      POSTGRES_DB: syncsphere
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U syncsphere']
      interval: 5s
      timeout: 5s
      retries: 10

  server:
    build:
      context: ./server
      dockerfile: ../docker/Dockerfile.server
    ports:
      - '3001:3001'
    environment:
      NODE_ENV: production
      PORT: '3001'
      DATABASE_URL: postgresql://syncsphere:syncsphere@postgres:5432/syncsphere
      DATABASE_SSL: 'false'
      JWT_SECRET: ${JWT_SECRET:-local-dev-jwt-token-signing-key-32-chars-minimum}
      JWT_EXPIRES_IN: 7d
      REFRESH_TOKEN_SECRET: ${REFRESH_TOKEN_SECRET:-local-dev-refresh-token-signing-key-32-chars-min}
      REFRESH_TOKEN_EXPIRES_IN: 30d
      CLIENT_URL: http://localhost:5173
      CLIENT_ORIGINS: http://localhost:5173
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://localhost:3001/health || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5

  client:
    build:
      context: ./client
      dockerfile: ../docker/Dockerfile.client
      args:
        VITE_API_URL: http://localhost:3001/api
        VITE_WS_URL: http://localhost:3001
    ports:
      - '5173:80'
    depends_on:
      server:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
